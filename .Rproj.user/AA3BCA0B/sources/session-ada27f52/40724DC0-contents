library(tidyverse)
library(gghighlight)
data <- mtcars 

data %>% 
  ggplot(aes(cyl, mpg)) +
  geom_bar(aes(fill = cyl), stat = "identity") +
  theme_light() +
  gghighlight(cyl > 4) +
  labs(title = "Example 1 - highlight without text") 
